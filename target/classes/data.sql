DROP TABLE IF EXISTS PESSOAS;

CREATE TABLE PESSOAS (
  ID INT AUTO_INCREMENT  PRIMARY KEY,
  NOME VARCHAR(250) NOT NULL,
  EMAIL VARCHAR(250),
  DT_NASCIMENTO DATE NOT NULL,
  NATURALIDADE VARCHAR(40),
  NACIONALIDADE VARCHAR(40),
  CPF VARCHAR(11) NOT NULL,
  AUD_DH_CRIACAO TIMESTAMP(6),
  AUD_DH_ALTERACAO TIMESTAMP(6),
  AUD_VERSAO NUMERIC(8,0) DEFAULT 0 NOT NULL,
  CONSTRAINT cpf_unique UNIQUE (cpf)
);

INSERT INTO PESSOAS (nome, dt_nascimento, cpf, aud_versao) VALUES
  ('Aliko', '1985-01-05', '66994420906', 0);